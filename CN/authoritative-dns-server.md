# Authoritative DNS Server

- An authoritative DNS server stores the official DNS records for a domain.

- It is the final source of truth.

- A `Recursive Resolver` finds the answer, and an `Authoritative Server` knows the answer.
