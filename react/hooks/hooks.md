# Hooks

- React Hooks are built-in (or custom) functions that let function components access React features like state, lifecycle, context, refs and performance optimizations without using class components